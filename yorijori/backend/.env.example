# 복사 후 .env 로 저장하고 값을 채우세요.
# cp .env.example .env
GEMINI_API_KEY=your_gemini_api_key_here

# (선택) YouTube가 봇으로 차단할 때 메타데이터 조회용. Google Cloud에서 YouTube Data API v3 사용 설정 후 키 발급.
# YOUTUBE_API_KEY=your_youtube_data_api_key_here

# (선택) 자막 없을 때 오디오 다운로드용. 하나만 설정하면 됨.
# 로컬에서 브라우저 쿠키 사용 (Chrome에 YouTube 로그인 필수):
# YT_DLP_BROWSER=chrome
# 또는 쿠키 파일 경로 (Netscape 형식): YT_DLP_COOKIES_PATH=/절대경로/cookies.txt
# 또는 내용 직접. Render 등에서는 붙여넣어도 한 줄로 들어갈 수 있음 → 서버가 자동 복구 시도.
# YT_DLP_COOKIES="..."
